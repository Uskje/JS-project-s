    <html> - задает начало HTML-документа.

    <head> - задает информацию о документе, такую как заголовок, мета-информацию, стили и скрипты.

    <title> - задает заголовок документа, который отображается во вкладке браузера.

    <body> - задает основное содержимое документа, такое как текст, изображения, таблицы, формы и т.д.

    <h1>-<h6> - задают заголовки различных уровней, которые используются для организации контента и определения его важности.

    <p> - задает абзац текста.

    <a> - задает гиперссылку на другой документ или веб-страницу.

    <img> - вставляет изображение на страницу.

    <ul> - задает ненумерованный список элементов.

    <ol> - задает нумерованный список элементов.

    <li> - задает элемент списка.

    <table> - создает таблицу.

    <tr> - задает строку в таблице.

    <td> - задает ячейку в таблице.

    <form> - создает форму для ввода данных.

    <input> - задает элемент для ввода данных, такой как текст, число, дата, время, флажок и т.д.

    <textarea> - задает многострочное текстовое поле для ввода.

    <button> - задает кнопку, которую можно нажимать для выполнения действия.

    <select> - создает выпадающий список для выбора одного или нескольких элементов.

    <option> - задает элемент списка в выпадающем списке.

    <div> - задает блок для группировки других элементов.

    <span> - задает инлайн-элемент для стилизации или группировки текста.

    <style> - задает стили для элементов на странице.

    <script> - задает скрипты, которые могут быть выполнены на странице.

    <meta> - задает мета-информацию о странице, такую как ключевые слова, описание, автор и т.д.
